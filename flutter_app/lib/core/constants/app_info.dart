/// Static app metadata shown on the Settings screen (About section).
///
/// Kept in sync with pubspec.yaml (name / version).
class AppInfo {
  AppInfo._();

  /// Display name of the application.
  static const String appName = 'DYS Financial Management System';

  /// Version from pubspec.yaml (`version: 1.0.0+1`).
  static const String appVersion = '1.0.0';
}
